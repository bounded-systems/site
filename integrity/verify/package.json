{
  "name": "@bounded-systems/verify",
  "version": "0.1.0",
  "description": "Standalone out-of-page verifier: cryptographically verifies a deployed site's published Sigstore bundle offline (sigstore-js) and re-hashes the served bytes.",
  "type": "module",
  "bin": {
    "bounded-verify": "./verify.mjs"
  },
  "license": "MIT",
  "dependencies": {
    "sigstore": "^5.0.0"
  }
}
