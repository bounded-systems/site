name: synoptic
# The whole site CI lives in synoptic (site-ci.yml). This repo just calls it + config.
on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:
permissions:
  contents: read
  id-token: write
jobs:
  site:
    uses: bounded-systems/synoptic/.github/workflows/site-ci.yml@v0.4.0
    permissions:
      contents: read
      id-token: write
